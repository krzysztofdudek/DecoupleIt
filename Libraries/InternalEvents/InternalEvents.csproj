<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <VersionPrefix>1.1.1</VersionPrefix>
        <Title>DecoupleIt - Internal events</Title>
        <Description>Library implements concept of application internal events allowing to decouple code and make it more "reactive" rather than "imperative".</Description>
        <PackageReleaseNotes>
            - allow only once to dispose InternalEventsScope
            - added methods for InternalEventsScope allowing to maintain storage
        </PackageReleaseNotes>
    </PropertyGroup>

    <Import Project="../netstandard.props" />

    <ItemGroup>
      <ProjectReference Include="..\DependencyInjection.Automatic\DependencyInjection.Automatic.csproj" />
      <ProjectReference Include="..\Tracing\Tracing.csproj" />
    </ItemGroup>

</Project>
