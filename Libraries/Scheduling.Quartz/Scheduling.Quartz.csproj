<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <VersionPrefix>2.0.0</VersionPrefix>
        <Title>DecoupleIt - Scheduling using Quartz</Title>
        <Description>Library uses Quartz as a scheduler for registered jobs using attributes from GS.DecoupleIt.Scheduling.</Description>
        <PackageReleaseNotes>
            - learned how dependencies work (now they not require the newet microsoft libs)
        </PackageReleaseNotes>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\Scheduling\Scheduling.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Quartz" Version="3.2.0" />
    </ItemGroup>

    <Import Project="../netstandard.props" />

</Project>
